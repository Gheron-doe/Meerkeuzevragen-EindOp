<UserControl x:Class="MV_WPF.Views.BulkImporterView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,5">
			<TextBlock Text="Test:" VerticalAlignment="Center" Margin="0,0,5,0" />
			<ComboBox Width="250" ItemsSource="{Binding Tests}" SelectedItem="{Binding SelectedTest}" DisplayMemberPath="Title" />
			<TextBlock Text="  Format:" VerticalAlignment="Center" Margin="10,0,5,0" />
			<ComboBox Width="80" ItemsSource="{Binding Formats}" SelectedItem="{Binding SelectedFormat}" />
			<Button Content="Refresh" Margin="10,0,0,0" Padding="10,2" Command="{Binding RefreshCommand}" />
		</StackPanel>

		<StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5,0,10">
			<TextBlock Text="File:" VerticalAlignment="Center" Margin="0,0,5,0" />
			<TextBox Width="500" Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}" />
			<Button Content="Browse" Margin="5,0,0,0" Padding="10,2" Command="{Binding PickFileCommand}" />
			<Button Content="Process" Margin="10,0,0,0" Padding="10,2" Command="{Binding ProcessCommand}" />
		</StackPanel>

		<DataGrid Grid.Row="2" ItemsSource="{Binding Results}" AutoGenerateColumns="False" IsReadOnly="True">
			<DataGrid.Columns>
				<DataGridTextColumn Header="UserId" Binding="{Binding UserId}" Width="80" />
				<DataGridTextColumn Header="Score" Binding="{Binding Score}" Width="60" />
				<DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="60" />
				<DataGridTextColumn Header="Wrong answers" Binding="{Binding Wrong}" Width="*" />
			</DataGrid.Columns>
		</DataGrid>

		<TextBlock Grid.Row="3" Text="{Binding Status}" Margin="0,10,0,0" Foreground="DarkSlateGray" />
	</Grid>
</UserControl>
