<UserControl x:Class="MV_WPF.ImportSourceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">

            <!-- Section 1: import question bank from a file -->
            <TextBlock Text="Import question bank" FontWeight="Bold" FontSize="14" Margin="0,0,0,8" />

            <WrapPanel Margin="0,0,0,8">
                <TextBlock Text="New topic:" VerticalAlignment="Center" Margin="0,0,5,0" />
                <TextBox x:Name="NewTopicBox" Width="200" />
                <Button Content="Add topic" Margin="10,0,0,0" Padding="10,2" Click="OnAddTopic" />
                <Button Content="Refresh"   Margin="40,0,0,0" Padding="10,2" Click="OnRefresh" />
            </WrapPanel>

            <WrapPanel Margin="0,0,0,8">
                <TextBlock Text="Topics (multiple):" VerticalAlignment="Center" Margin="0,0,5,0" />
                <ComboBox x:Name="ImportTopicsCombo" Width="200" Height="26">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="  Format:" VerticalAlignment="Center" Margin="10,0,5,0" />
                <ComboBox x:Name="ImportFormatCombo" Width="80" />

                <TextBlock Text="  Difficulty:" VerticalAlignment="Center" Margin="10,0,5,0" />
                <ComboBox x:Name="ImportDiffCombo" Width="55" />
            </WrapPanel>

            <WrapPanel Margin="0,0,0,4">
                <TextBlock Text="File:" VerticalAlignment="Center" Margin="0,0,5,0" />
                <TextBox x:Name="ImportFileBox" Width="440" />
                <Button Content="Browse" Margin="5,0,0,0"  Padding="10,2" Click="OnPickFile" />
                <Button Content="Import" Margin="10,0,0,0" Padding="15,2" Click="OnImport" />
            </WrapPanel>

            <Separator Margin="0,16,0,16" />

            <!-- Section 2: bulk import student attempts from a CSV file -->
            <TextBlock Text="Import bulk attempts from file"
                FontWeight="Bold" FontSize="14" Margin="0,0,0,8" />
            <TextBlock TextWrapping="Wrap" Foreground="DarkSlateGray" Margin="0,0,0,8"
                Text="Select a test and a CSV file. Format: UserId,Answers,(optional Feedback). Everything is saved in a single transaction." />

            <WrapPanel Margin="0,0,0,8">
                <TextBlock Text="Test:" VerticalAlignment="Center" Margin="0,0,5,0" />
                <ComboBox x:Name="BulkTestCombo" Width="220" DisplayMemberPath="Title" />

                <TextBlock Text="  Format:" VerticalAlignment="Center" Margin="10,0,5,0" />
                <ComboBox x:Name="BulkFormatCombo" Width="80" />
            </WrapPanel>

            <WrapPanel Margin="0,0,0,8">
                <TextBlock Text="File:" VerticalAlignment="Center" Margin="0,0,5,0" />
                <TextBox x:Name="BulkFileBox" Width="440" />
                <Button Content="Browse" Margin="5,0,0,0"  Padding="10,2" Click="OnBulkPickFile" />
                <Button Content="Import" Margin="10,0,0,0" Padding="10,2" Click="OnBulkImport" />
            </WrapPanel>

            <DataGrid x:Name="BulkResultsGrid" AutoGenerateColumns="False"
                IsReadOnly="True" CanUserAddRows="False" MaxHeight="250" Margin="0,0,0,8">
                <DataGrid.Columns>
                    <DataGridTextColumn     Header="UserId"          Binding="{Binding UserId}"       Width="90" />
                    <DataGridTextColumn     Header="Correct"         Binding="{Binding CorrectCount}" Width="55" />
                    <DataGridTextColumn     Header="Total"           Binding="{Binding Total}"        Width="50" />
                    <DataGridCheckBoxColumn Header="OK"              Binding="{Binding Persisted}"    Width="40" />
                    <DataGridTextColumn     Header="Wrong questions" Binding="{Binding Wrong}"        Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock x:Name="StatusText" Foreground="DarkSlateGray" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
